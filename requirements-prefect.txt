# Optional orchestration layer. Install alongside requirements.txt to run the
# Prefect flows (translator/workflow/flows.py) and the monitoring UI:
#     pip install -r requirements.txt -r requirements-prefect.txt
#     prefect server start        # UI at http://127.0.0.1:4200
# The core skills and the deterministic pipeline.py runner do NOT need this.
prefect>=3,<4
